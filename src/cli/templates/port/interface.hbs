export interface {{namePascal}}Port {
	// Example method signatures (uncomment and modify as needed):
	// get{{namePascal}}(id: string): Promise<{{namePascal}}>
	// create{{namePascal}}(data: Create{{namePascal}}Dto): Promise<{{namePascal}}>
	// update{{namePascal}}(id: string, data: Update{{namePascal}}Dto): Promise<{{namePascal}}>
	// delete{{namePascal}}(id: string): Promise<void>
}
