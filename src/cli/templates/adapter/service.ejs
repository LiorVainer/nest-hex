/**
 * <%= namePascal %> Service Implementation
 *
 * This service implements the <%= portName || namePascal %>Port interface<% if (technology) { %> using <%= technology %><% } %>.
 */

import { Injectable } from '@nestjs/common'
import type { <%= portInterfaceName || `${namePascal}Port` %> } from '<%= portInterfaceImport || `../../${portNameKebab || nameKebab}.port` %>'
import type { <%= namePascal %>Options } from './<%= nameKebab %>.types'

/**
 * <%= namePascal %> service implementation.
 */
@Injectable()
export class <%= namePascal %>Service implements <%= portInterfaceName || `${namePascal}Port` %> {
	constructor(private readonly options: <%= namePascal %>Options) {}

	/**
	 * TODO: Implement <%= portInterfaceName || `${namePascal}Port` %> interface methods here
	 *
	 * Example:
	 * async getData(id: string): Promise<Data> {
	 *   // Implementation using this.options
	 * }
	 */
}
